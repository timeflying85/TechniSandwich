spring:
  datasource:
    url: jdbc:postgresql://localhost:6969/techni-sandwich
    username: postgres
    password: '***'
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
      format_sql: true
